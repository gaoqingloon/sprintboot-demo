<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="testDao">
    <select id="getTestDBInfo" resultType="java.util.Map">
        select id,username,password,email from test.`user` where id=#{id}
    </select>
</mapper>
